import { EditRightPageDirective } from './edit-right-page.directive';

describe('EditRightPageDirective', () => {
  it('should create an instance', () => {
    const directive = new EditRightPageDirective();
    expect(directive).toBeTruthy();
  });
});
